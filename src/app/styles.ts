import {
  classnames,
  minHeight,
  width,
  backgroundColor,
} from "../shared/ui/classnames";

export const app = classnames(
  minHeight("min-h-screen"),
  width("w-screen"),
  backgroundColor("bg-dark")
);
